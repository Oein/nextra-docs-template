# Any (무엇이든!)

Typescript에서 Javascript를 사용하기 위해 any를 사용해요. any에는 어떤 type이든 들어갈 수 있어요.

```ts
let g: any;
g = 1;
console.log(g);
g = "String";
console.log(g);
g = false;
console.log(g);

> 1
> "String"
> false
```
